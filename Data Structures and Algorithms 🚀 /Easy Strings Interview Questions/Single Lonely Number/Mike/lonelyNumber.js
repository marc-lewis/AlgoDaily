/** 
 * Take an array of numbers and return the number with only one instance
 * @param {number} num
 * @return {Boolean}
*/

function lonelyNumber(numbers) {
  // Fill in this method
  return numbers;
}
